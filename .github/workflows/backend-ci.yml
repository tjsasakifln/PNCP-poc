name: Backend CI

on:
  pull_request:
    paths:
      - 'backend/**'
      - '.github/workflows/backend-ci.yml'
  push:
    branches:
      - main
      - develop

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: |
          cd backend
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run linting
        run: |
          cd backend
          ruff check .

      - name: Run security vulnerability scan
        run: |
          cd backend
          pip install safety
          # Scan for known vulnerabilities (allow to fail for now)
          safety check --json || echo "⚠️ Vulnerabilities found - review required"

      - name: Run mypy type checking
        run: |
          cd backend
          pip install mypy
          mypy . --config-file pyproject.toml || echo "::warning::mypy found type errors — review required"

      - name: Run tests with coverage
        run: |
          cd backend
          pytest --cov --cov-report=xml --cov-report=term --cov-fail-under=70

      - name: Upload coverage to Codecov (optional)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: codecov/codecov-action@v4
        with:
          file: ./backend/coverage.xml
          flags: backend
          fail_ci_if_error: false
